/**
 * Stub Posts component.
 * Replace this with your real blog‑listing UI when you are ready.
 */
export function Posts(_props: { range?: [number, number?]; columns?: string }) {
  return null; // render nothing until blog posts are added
}